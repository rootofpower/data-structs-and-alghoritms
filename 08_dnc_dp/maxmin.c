#include "maxmin.h"

#ifndef MAX
#define MAX(a,b) ((a)>(b)?a:b)
#endif
#ifndef MIN
#define MIN(a,b) ((a)<(b)?a:b)
#endif

pair MaxMin(int A[], int L, int H)
{
	pair ret1, ret2, ret;
	ret.min = A[L];
	ret.max = A[L];
	
	// Doplnit ...
    
	return ret;
}
